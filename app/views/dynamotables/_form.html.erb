<%= form_for(@dynamotable) do |f| %>
  <% if @dynamotable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dynamotable.errors.count, "error") %> prohibited this bucket from being saved:</h2>

      <ul>
      <% @dynamotable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <i>No spaces in name allowed</i>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
